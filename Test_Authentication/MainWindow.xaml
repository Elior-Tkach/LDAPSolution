<Window x:Class="Test_Authentication.AuthenticateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Authenticate User" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Username:" Width="100"/>
            <TextBox x:Name="UsernameBox" Width="200"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Password:" Width="100"/>
            <PasswordBox x:Name="PasswordBox" Width="200"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,10">
            <Label Content="Permission:" Width="100"/>
            <TextBox x:Name="PermissionBox" Width="200"/>
        </StackPanel>
        <Button Grid.Row="3" Content="Authenticate" Width="120" Click="Authenticate_Click" HorizontalAlignment="Left"/>
        <TextBlock x:Name="ResultBlock" Grid.Row="4" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</Window>
