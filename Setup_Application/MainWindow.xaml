<Window x:Class="Setup_Application.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Setup_Application"
        Title="LDAP Application"
        Height="750" Width="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="0,65,50,-6">
        <Grid.RowDefinitions>
            <RowDefinition Height="27*"/>
            <RowDefinition Height="35*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="34*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TabControl x:Name="MainTabControl" Margin="10,-61,-45,10" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <TabItem Header="Connection">
                <local:ConnectionWithServer x:Name="connectionControl" Height="694" Margin="0,0,-46,0" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>
