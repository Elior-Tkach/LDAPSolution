<UserControl x:Class="Setup_Application.ConnectionWithServer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
     mc:Ignorable="d" 
    d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <Style x:Key="HighlightableButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Tag" Value="Highlighted">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Faded style for disabled buttons -->
        <Style x:Key="FadedButtonStyle" TargetType="Button" BasedOn="{StaticResource HighlightableButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                    <Setter Property="Foreground" Value="Gray"/>
                    <Setter Property="Background" Value="LightGray"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Remove duplicate DataTemplate with same key -->
        <DataTemplate x:Key="TreeViewHeaderTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Name}" />
                <TextBlock Text=" " />
                <TextBlock Text="{Binding TypeLabel}" Foreground="Blue" FontWeight="Bold"/>
            </StackPanel>
        </DataTemplate>
        <HierarchicalDataTemplate x:Key="TreeViewHeaderHierarchicalTemplate" ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Name}" />
                <TextBlock Text=" " />
                <TextBlock Text="{Binding TypeLabel}" Foreground="Blue" FontWeight="Bold"/>
            </StackPanel>
        </HierarchicalDataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="Accellix logo2.png" Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="IP address/ host name:" Grid.Row="4" Grid.Column="0" FontSize="26" Margin="20,100,10,224"/>
        <TextBox x:Name="HostTextBox" Grid.Row="4" Grid.Column="1" FontSize="24" Height="54" Margin="39,76,340,470" TextChanged="InputFieldsChanged"/>
        <Button x:Name="TestConnectionBtn" Content="Test connection" Grid.Row="4" Grid.Column="1" FontSize="21" Width="181" Height="38" Margin="468,67,-40,495" Click="TestConnectionBtn_Click" Style="{StaticResource HighlightableButtonStyle}" Grid.ColumnSpan="2"/>
        <Button x:Name="SaveServerBtn" Content="Save the server" Grid.Row="4" Grid.Column="1" FontSize="21" Width="181" Height="36" Margin="468,125,-40,439" Click="SaveServerBtn_Click" Style="{StaticResource HighlightableButtonStyle}" Grid.ColumnSpan="2"/>
        <TextBlock Text="username:" Grid.Row="4" Grid.Column="0" FontSize="26" Margin="160,170,218,152" Grid.ColumnSpan="2"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="4" Grid.Column="1" FontSize="24" Height="56" Margin="39,150,340,394" TextChanged="InputFieldsChanged"/>
        <TextBlock Text="password:" Grid.Row="4" Grid.Column="0" FontSize="26" Margin="160,238,218,88" Grid.ColumnSpan="2"/>
        <PasswordBox x:Name="PasswordTextBox" Grid.Row="4" Grid.Column="1" FontSize="24" Height="52" Margin="39,226,340,322" PasswordChanged="InputFieldsChanged"/>
        <Button x:Name="ContinueBtn" Content="Continue to setup" Grid.Row="4" Grid.Column="1" FontSize="23" Width="240" Height="48" Margin="474,226,-105,326" Click="ContinueBtn_Click" IsEnabled="False" Style="{StaticResource FadedButtonStyle}" Grid.ColumnSpan="2"/>
        <Border x:Name="ErrorTextBoxBorder" Grid.Row="4" Grid.ColumnSpan="3" BorderBrush="Black" BorderThickness="2" Visibility="Collapsed" Height="35" Margin="40,292,10,273">
            <TextBox x:Name="ErrorTextBox" FontSize="20" Visibility="Collapsed" IsReadOnly="True" BorderThickness="0" Background="Transparent" Margin="8,0,0,-2" HorizontalAlignment="Left" Width="840" />
        </Border>
        <Image x:Name="StatusIcon" Visibility="Collapsed" Grid.Row="4" Grid.Column="0" Width="24" Height="32" Margin="15,327,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" />
    </Grid>
</UserControl>